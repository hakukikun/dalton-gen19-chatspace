<div class="row">
<div class="grid-6">
  <% @posts.each do |post| %>
    <%= render partial: "post", locals: { post: post } %>
  <% end %>
    <strong> <%= paginate(@posts) %> </strong>
  </div>
</div>
</div>
